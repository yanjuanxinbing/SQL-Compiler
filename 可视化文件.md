# 《大型平台软件设计实习》MiniDB可视化界面开发方案

> 依据指导书、PPT、评分文档提取可视化相关需求，结合项目原有MiniDB后端架构，设计GUI可视化界面，作为**项目亮点扩展模块**（评分文档可作为加分项：数据库Web/GUI管理界面、查询计划可视化）。

## 一、原始文档提取可视化相关要点
1. **SQL编译器输出**：Token流（种别码、词素、行号、列号）、AST抽象语法树、语义检查结果、逻辑执行计划（树形/JSON/S表达式）、优化后执行计划。
2. **存储系统输出**：缓存命中统计、页替换日志、页分配/释放状态、脏页信息。
3. **数据库引擎输出**：SQL执行结果集、系统Catalog元数据（表、字段、类型）。
4. **扩展亮点需求**：EXPLAIN执行计划可视化、计划优化前后对比；缓存替换策略可视化；可作为创新加分模块。

## 二、实际实现方案

### 技术选型
- **后端**：Python FastAPI（已有） + 扩展 `engine.py`/`api.py`/`schemas.py`
- **前端**：Vanilla JS + 扩展 `app.js`/`style.css`/`index.html`（复用现有webui架构）
- **C++引擎**：新增 `--debug-output` flag，直接输出JSON调试包

### 架构图
```
用户浏览器 <--> FastAPI (扩展) <--> C++ sqlcompiler.exe --debug-output
                                           |
                                   Database::LastTokens()
                                   Database::LastAst()
                                   Database::LastPlan()->ToJson()
                                   Database::LastPlanBeforeOptText()
                                   Storage().GetStats()
```

### 核心原则
**不改动原有编译器/存储引擎核心逻辑**，只新增 `--debug-output` CLI flag，把调试数据以JSON格式输出。

## 三、已实现的界面功能模块

在现有3-tab (Query/Schema/History) 基础上新增 **Visualize tab**，包含5个可视化模式：

### 模块1：Tokens（词法分析）
- Token流表格：Type/Lexeme/Line/Col
- 关键词/标识符/字面量/运算符按类型着色

### 模块2：AST（语法分析）
- 缩进树形可视化，解析 AST ToString() 输出
- 点击节点可选（可扩展）

### 模块3：Plan（执行计划）
- JSON格式执行计划树可视化
- 算子节点按类型着色（SeqScan=青、Filter=黄、Project=绿等）
- 支持原始JSON文本查看

### 模块4：Compare（优化前后对比）
- 左右分栏布局
- 左边：优化前执行计划文本
- 右边：优化后执行计划树

### 模块5：Storage（存储系统监控）
- 缓存命中率统计卡片（Hit Rate/Hits/Misses/Replacements/Total Pages）
- 命中率进度条
- 页替换日志列表（evict/load/dirty事件）

## 四、后端接口设计

### 核心接口
| Method | Path | 说明 |
|--------|------|------|
| POST | `/api/query/debug` | 执行SQL + 返回完整调试数据（tokens/ast/plan/stats） |
| GET | `/api/storage/stats` | 返回BufferPoolStats（命中/未命中/替换次数/日志） |

### schemas.py 新增模型
- `TokenInfo`: type/lexeme/line/col
- `StorageStats`: hit_count/miss_count/replacement_count/hit_rate/total_pages
- `ReplacementEntry`: evicted/loaded/dirty
- `DebugData`: tokens/ast_text/plan_json/plan_before_opt/storage_stats/replacement_log
- `DebugRequest` / `DebugResponse`
- `StorageStatsResponse`

### engine.py 新增方法
- `execute_debug(statement)`: 调用 C++ `--debug-output` flag，解析 `[DEBUG_JSON_START]...[DEBUG_JSON_END]` JSON块

### C++ main.cpp 新增
- `--debug-output` flag：脚本模式下每条SQL后输出JSON调试包
- `--debug` flag：REPL模式下每条SQL后输出JSON调试包
- `PrintDebugJson()` 函数：输出完整调试数据JSON

## 五、关键文件清单

| 文件 | 改动 |
|------|------|
| `src/main.cpp` | 新增 `--debug-output`/`--debug` flag 和 `PrintDebugJson()` 函数 |
| `webui/backend/schemas.py` | 新增 DebugData/StorageStats/ReplacementEntry 等Pydantic模型 |
| `webui/backend/engine.py` | 新增 `execute_debug()` 方法 |
| `webui/backend/api.py` | 新增 `/api/query/debug` 和 `/api/storage/stats` 端点 |
| `webui/backend/static/index.html` | 新增 Visualize tab 和 viz-toolbar/viz-content 布局 |
| `webui/backend/static/style.css` | 新增可视化组件样式（token表/plan树/compare布局/storage卡片） |
| `webui/backend/static/app.js` | 新增 vizState/runViz()/renderTokens()/renderPlan() 等渲染函数 |

## 六、测试用例

### 正常SQL测试
```sql
CREATE TABLE student(id INT, name VARCHAR, age INT);
INSERT INTO student(id,name,age) VALUES (1,'Alice',20);
SELECT id,name FROM student WHERE age > 18;
```
> 预期：Token流表格、AST树、执行计划树（优化前后对比）、存储统计。

### 语义错误测试
```sql
SELECT not_exist_col FROM student;
```
> 预期：AST/Plan面板展示编译失败信息。

### 计划优化测试
```sql
SELECT name FROM student WHERE 1=1 AND age>10+8;
```
> 预期：Compare视图优化前Filter条件`1=1 AND age>10+8`，优化后`age>18`。

## 七、答辩与报告注意点
1. 报告写可视化模块设计思路、架构图、关键界面截图。
2. 现场演示：Token流、AST树、执行计划优化对比、缓存监控。
3. 说明技术难点：AST/Plan树JSON序列化、C++到Python的数据传递、左右分栏对比布局。
4. 说明**不改动原有编译器/存储引擎核心逻辑**，可视化只是表现层。
