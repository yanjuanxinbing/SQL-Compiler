# 文件处理动作清单（2026-09-14 全面复核）

- 依据：用户要求"复核项目目录结构、识别所有需上传与合并的文件、为每个文件确定操作（上传/合并/其他），记录文件列表与对应动作"
- 时机：操作系统页面管理模块展示前最终复核（2026-09-14）
- 当前分支状态：`os-page-management` @ 284168e，工作区干净；远端 `origin/os-page-management` 已同步

---

## 1. 操作分类总览

| 类别 | 数量 | 操作 | 说明 |
|---|---|---|---|
| A. 已上传（无需再处理） | 64 文件 | ✅ 已推送 | `e45f1fc`（61）+ `284168e`（3）均已 `git push` 至 `origin/os-page-management` |
| B. 仅本分支独有、待上传（应在合并后保留） | 209 文件 | ⏸ 暂缓 | 见 §3，与 main 合并后保留；本分支独有**新增源文件为 0** |
| C. 仅 main 分支独有、与本分支无交集 | 180 文件 | ⏸ 暂缓 | 见 §4，展示后按 doc 16 预案合并入本分支 |
| D. 工作区未追踪的运行时产物 | 19 文件 | ⏸ 卫生修正 | 临时数据库文件 / 测试残留日志 / 临时目录，本次补 .gitignore（见 §6） |
| E. 工具脚本与文档（已上传） | — | ✅ | `tests/run_bench.ps1` / `tests/run_gc_sweep.ps1` / `tests/sql/53_subquery_decorrelate.sql` 均在 e45f1fc |

> 结论：**所有源码、文档、测试脚本均已提交并推送远端**。D 类产物通过补 .gitignore 隔离即可。本次不执行 main → 本分支深度合并（按用户"暂缓"决策）。

## 2. A 类：已上传文件清单

### 2.1 `e45f1fc`（feat(storage): Phase6 U1b-U3 查询优化与收官，61 文件）

**新增**：
- `docs/test_evidence/bench_sweep.log`、`final_verify.log`、`gc_sweep.log`、`join_reorder_u3_1.log`、`preagg_pushdown_u3_2.log`、`storage_ut_u1d.log`、`storage_ut_u2_2.log`、`storage_ut_u3_2.log`、`subquery_decorrelate_u3_3.log`（9 份阶段测试证据）
- `docs/upload_os_module/10_Phase6_U1_索引删除合并与再平衡_阶段文档.md`
- `docs/upload_os_module/11_Phase6_U3_查询优化增强_阶段文档.md`
- `docs/upload_os_module/12_模块功能测试用例明细.md`
- `docs/upload_os_module/13_模块功能展示文档.md`
- `docs/upload_os_module/14_核心机制备选方案对比分析.md`
- `docs/upload_os_module/15_展示流程与脚本.md`
- `include/execution/PreAggScanExecutor.h`、`src/execution/PreAggScanExecutor.cpp`（聚合下推执行器）
- `tests/run_bench.ps1`、`tests/run_gc_sweep.ps1`（基准与组提交调参脚本）
- `tests/sql/53_subquery_decorrelate.sql`（子查询去关联回归用例）

**修改**（51 个）：`.gitignore`、`include/ast/AST.h`、`include/catalog/SystemCatalog.h`、`include/db/Database.h`、`include/execution/ExecutionEngine.h`、`include/execution/Executor.h`、`include/index/BPlusTree.h`、`include/optimizer/Optimizer.h`、`include/plan/Plan.h`、`include/storage/LockManager.h`、`include/storage_engine/TableHeap.h`、`src/catalog/SystemCatalog.cpp`、`src/db/Database.cpp`、`src/execution/CteExecutor.cpp`、`src/execution/ExecutionEngine.cpp`、`src/execution/Executor.cpp`、`src/execution/ExpressionEvaluator.cpp`、`src/execution/JoinExecutor.cpp`、`src/index/BPlusTree.cpp`、`src/optimizer/Optimizer.cpp`、`src/plan/Plan.cpp`、`src/storage/LockManager.cpp`、`src/storage_engine/TableHeap.cpp`、`tests/storage/storage_ut.cpp`（+1136 行）等；以及 docs/upload_os_module/00/01/03/05/08/09 与 docs/test_evidence/ 既有日志。

### 2.2 `284168e`（docs(storage): 新增16_代码同步与合并预案，3 文件）

- 新增：`docs/upload_os_module/16_代码同步与合并预案.md`
- 修改：`docs/upload_os_module/00_总览_索引.md`、`docs/upload_os_module/05_文档整合归类.md`

## 3. B 类：本分支独有、待合并后保留（209 文件，按类别）

> 命令：`git rev-list --objects origin/main..origin/os-page-management` 全部 209 项已纳入 `e45f1fc` + `284168e` 两个提交历史中；远端已接收，等待 main 合并完成后按 doc 16 §6 步骤保留。

| 类别 | 文件数 | 代表 |
|---|---|---|
| 新源码 | **0** | 无 — 本分支没有引入 main 完全没有的新源文件 |
| 文档（`.trae/documents/`、`docs/`） | 24 | BPlusTree_Optimistic_Page_Concurrency.md / MVCC 系列 / Phase4_PredicateTree_GroupCommit_TempFlush.md / OS_Module_Development_Plan.md 等 |
| 上传包文档 | 17 | `docs/upload_os_module/00-16` |
| 历史验收与报告 | 9 | `Isolation_DeepDev_Summary.md` / `Storage_Subsystem_Acceptance_Report.md` / `Phase3/Phase4_Acceptance_Report.md` / `Optimization_Stage_Report.md` 等 |
| 现有源文件修改（双方都改，合并冲突） | ~30 | TableHeap.cpp / Database.cpp / Optimizer.cpp / ExecutionEngine.cpp / BPlusTree.cpp / Executor.cpp 等 |
| 测试 SQL | 53 | `tests/sql/00-53_*`（本分支编号体系） + `tests/storage/storage_ut.cpp` |
| 测试脚本 | 6 | `tests/run_repro_all.ps1` / `tests/run_param_sweep.ps1` / `tests/run_acid_clr.bat` 等 |
| 计划/教程/优化文档 | 10 | `OS_Module_Optimization_Proposal.md` / `T5_Delivery_Checklist.md` / `T5_Development_Summary.md` 等 |
| `.gitignore` 增强 | 1 | 本分支版本含 8 行（main 端是 3 行，合并取并集） |

## 4. C 类：main 分支独有、与本分支无交集（180 文件）

> 命令：`git rev-list --objects origin/os-page-management..origin/main`。这些文件**不存在于本分支**，是队友 9 个提交新增内容。展示后合并时按 doc 16 预案保留入本分支。

| 类别 | 数量 | 代表 |
|---|---|---|
| **新源代码（执行器与基础设施）** | **16 对头+实现** | `ApplyExecutor`、`CallExecutor`、`MaterializedViewExecutor`、`MergeExecutor`、`SchemaSequenceExecutor`、`TimingProxyExecutor`、`ValuesExecutor`、`StorageAccess`、`TypeCoercion`、`EditDistance`、`SemanticErrorStage` |
| 新测试脚本 | 4 | `run_debug_meta.bat` / `run_plan_optimized.bat` / `run_tokens_complete.bat` / `run_trigger_persistence.bat` |
| 新测试 SQL | 33 | `tests/sql/52_data_types.sql` ~ `tests/sql/91_order_by_ordinal_cast_default.sql`（重编号三/两位序） |
| 删除：原 `07_alias.sql`、`err_*` 测试 | 2-3 | main 已删除的旧测试 |
| 修改（双方都改，合并冲突） | ~30 | 与 B 类对称：Database.cpp / TableHeap.cpp / BPlusTree.cpp / Optimizer.cpp / ExecutionEngine.cpp 等 |
| `.gitignore` 差异 | 1 | main 端 3 行版本（不包含 sweep/bench/gc 模式） |

## 5. 合并冲突规模重新核算

之前 doc 16 评估 "28 文件 75 处冲突" 是基于 `git merge --no-commit` 实测，对合并冲突标记数（Hunk 级）。重新对照：

- **文件级冲突数：28 个文件**（双方都改的不同代码块）
- **Hunk 级冲突标记：~75 处**（如 TableHeap.cpp 22 处、BufferPoolManager.cpp 4 处）
- **本分支独有 vs main 独有**：209 vs 180 项（rev-list --objects 统计）

> 此冲突规模在合并工作流中属于"中等偏高但可控"区间——doc 16 §6 已给出逐文件解决预案（含回退保险）。

## 6. D 类：工作区未追踪运行时产物（19 项，需 .gitignore 修正）

实测清单（在 `main` 分支工作区快照，git 切换到 os-page-management 后自动消失）：

```text
build_audit/                         ← 构建产物目录
sqlcompiler.db / .db.crc / .db.wal   ← 数据库运行时产物
storage_ut_bg_flush.bin.crc          ← storage_ut 测试残留
storage_ut_bpm.bin.crc
storage_ut_bpm_cap.bin.crc
storage_ut_bpm_clock.bin.crc
storage_ut_conc.bin.crc
storage_ut_crc.bin.fpl
storage_ut_disk.bin.crc
storage_ut_disk.bin.fpl
storage_ut_err.log                   ← storage_ut 测试日志
storage_ut_fpl.bin.crc
storage_ut_io_stats.bin.crc
storage_ut_run.log
tests/tmp_bench/                     ← 基准测试临时目录
tests/tmp_bench2/
tests/tmp_gc2/
tests/tmp_sweep2/                    ← 参数扫描临时目录
```

**操作**：在 `.gitignore` 中追加以下模式（合并现有 8 行版本）：

```gitignore
/build
/.vscode
/tests/tmp
/tests/tmp_sweep*
/tests/tmp_bench*
/tests/tmp_gc*
/build_audit/
/storage_ut_*.bin.crc
/storage_ut_*.bin.fpl
/storage_ut_*.log
/sqlcompiler.db*
/sqlcompiler.db
*.log
```

`.log` 全局模式替代根目录规则（原版本用根目录 `*.log`，文件级 `.gitignore` 不支持根相对规则）。随后 `git clean -fd` 验证无工作区差异。

## 7. 动作清单（按执行优先级）

| # | 动作 | 工具 | 状态 |
|---|---|---|---|
| 1 | 提交并推送 e45f1fc（Phase6 U1b-U3 全部成果 + 收官文档包） | `git add/commit/push` | ✅ |
| 2 | 提交并推送 284168e（合并预案文档 16） | `git add/commit/push` | ✅ |
| 3 | 拉取全部远端引用、本地 main 对齐 origin/main（107114b） | `git fetch origin` | ✅ |
| 4 | 实测 merge 冲突规模并写入 doc 16 | `git merge --no-commit --no-ff origin/main` | ✅ |
| 5 | 拉取测试证据链入 docs/test_evidence/（9 份阶段日志） | 已在 e45f1fc | ✅ |
| 6 | 本次：编写 doc 17 文件处理动作清单 | Write tool | 进行中 |
| 7 | 本次：补 .gitignore 卫生修正（隔离 D 类产物） | Edit → commit → push | 待执行 |
| 8 | 本次：构建 + storage_ut 冒烟验证基线 | cmake / build / storage_ut | 待执行 |
| 9 | 展示后：按 doc 16 §6 预案合并 main | rebase-or-merge + 28 文件逐项 | 暂缓 |
| 10 | 展示后：回归全量（run_repro_all + main 侧脚本）并产出 merge_main_<date>.log | PowerShell | 暂缓 |

## 8. 关键风险与降级

- **重新合并时若出现 28 文件 75 处冲突外的增量冲突**：先 `git merge --abort` 回退，按 doc 16 §6 步骤 0-7 重新评估；保留 origin/os-page-management@284168e 全绿基线作为回滚锚点。
- **当前基线不可破坏**：本次所有操作（doc 17 / .gitignore 修正 / 冒烟）结束后必须 `git status --short` 输出空、`git log --oneline -1` 仍为 284168e 或其直接后继。

## 9. 版本控制规范

- 提交信息遵循「`scope(module): 动作 — 说明 — 关键改动`」三段式（如本分支所有提交）。
- 每次合并推送前运行：`git fetch origin && git status --short` 确认无意外修改。
- 推送后保留 `git reflog` 输出至少 24h（已在 §1 复核时记录 284168e HEAD@{0} 锚点）。