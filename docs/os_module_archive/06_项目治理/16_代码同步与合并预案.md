# 代码同步与合并预案（收官版）

- 分支：`os-page-management`（操作系统页面管理模块成果线）
- 日期：2026-09-14
- 结论：**本分支成果已推送远端（e45f1fc）；`main` 最新更新已拉取到本地引用；深度合并经评估后暂缓至展示后执行**，本文件给出完整合并预案，确保后续合并可控、可回退、可验证。

---

## 1. 同步结论与决策依据

| 事项 | 状态 | 说明 |
|---|---|---|
| 本分支全部修改提交并推送 | ✅ 完成 | `e45f1fc`（61 文件，+5660/-119），远端 `origin/os-page-management` 已同步 |
| 拉取其他分支最新更新 | ✅ 完成 | `git fetch origin` 已更新全部远程跟踪引用；本地 `main` 已对齐 `origin/main`（107114b） |
| 深度合并 `main` → 本分支 | ⏸ 暂缓 | 经实测合并产生 **28 文件 ~75 处冲突**（见 §3），且 `main` 含引擎层架构重构（见 §4），在展示前夜强行合并的回归风险不可接受；决策：**展示使用当前全绿基线，展示后按 §6 预案执行合并** |

决策过程：对 `origin/main` 执行 `git merge --no-commit --no-ff` 实测，冲突规模与性质评估后确认完整合并+回归难以在展示前可靠完成，故回退（`git merge --abort`），工作区恢复干净基线。

## 2. 分支拓扑与各分支最新状态（2026-09-14 抓取）

```text
               589bfe4 (合并基：CMakeLists/AST/执行器/存储/事务初始线)
               ├─────────── main 线（数据库引擎主线，活跃更新）
               │             7f1fc18 引擎重构（删 codegen/加 StorageAccess/TimingProxy/语义错误分级）
               │             4d82fb7 执行器扩展（Apply/Call/MaterializedView/Merge/Values/触发器持久化）
               │             …（2ea4a95/500184e/1621250/dc7b79b/0408b58/9785684 功能与测试）
               │             107114b 新增 run_plan_optimized.bat（最新，队友刚推送）
               │
               └─────────── os-page-management 线（本模块）
                            17943d8 页式存储缓冲池基础
                            d0c1d18 MVCC/锁管理
                            f848d05 并发控制/优化落地与文档包
                            …（Phase5 周期2-4/T4/T5 收口）
                            e45f1fc Phase6 U1b-U3 查询优化与收官（本次推送）
```

| 分支 | 最新提交 | 与本分支关系 |
|---|---|---|
| `origin/main` | 107114b | 领先本分支 10 提交（含 2 个新提交：9785684/107114b） |
| `origin/os-page-management` | e45f1fc | 本分支，已推送 |
| `origin/cheng` | 9d3e02b（完成了2.3.4） | 独立成果线 |
| `origin/ckm` | 7a4bf56（测试重编号） | 独立成果线 |
| `origin/feature/codegen-5and7` | e33782d | 早期 codegen 试验线（main 已删除 codegen，此线已过时） |

## 3. 合并冲突清单（实测 2026-09-14）

对 `origin/main` 执行 `git merge --no-commit --no-ff` 实测结果：28 个文件冲突，共约 75 处冲突标记。

| 文件 | 冲突 hunk 数 | 冲突性质 |
|---|---|---|
| `src/storage_engine/TableHeap.cpp` | 22 | main 引入类型强制转换/MVCC 模式（70_mv_schema）重构；本分支有快照版本链/索引墓碑回收/内联真空改动——**最深冲突区** |
| `src/storage/BufferPoolManager.cpp` | 4 | main 2ea4a95 调整；本分支 Phase5 温度感知刷盘/自适应阈值/后台刷脏 |
| `src/execution/UpdateExecutor.cpp` | 4 | main 类型强制转换；本分支 UPSERT/FCW/快照读改动 |
| `src/db/Database.cpp` | 4 | main 重构（StorageAccess/TimingProxy/删 codegen/新增 bench 入口）；本分支子查询缓存统计注入/`\bench`/`\stats` 扩展 |
| `include/db/Database.h` | 4 | 同上 |
| `src/index/BPlusTree.cpp` | 3 | main 7f1fc18 改动；本分支 U1b/c/d 合并/物理删页/TryFreePage |
| `src/execution/DeleteExecutor.cpp` | 3 | main 类型强制转换；本分支 READ COMMITTED 锁释放/快照读 |
| `src/execution/ExecutionEngine.cpp` | 3 | main 新增执行器分支；本分支 PRE_AGG_SCAN 分支 + 谓词锁注册 |
| `src/execution/SeqScanExecutor.cpp` | 2 | main 改动；本分支快照挂载/谓词注册 |
| `src/optimizer/Optimizer.cpp` | 2 | main 重构 IndexAccessPath；本分支 3 个优化 pass（重排/下推/去关联） |
| `src/main.cpp` | 2 | main 新增 debug_meta/plan_optimized；本分支 CLI 特性 |
| `src/plan/Plan.cpp`、`Planner.cpp` | 各 2 | main 计划重构；本分支 SEMI/ANTI/PreAggScan 计划节点 |
| `include/execution/Executor.h`、`Executor.cpp` | 各 2 | 执行器注册表双方增项 |
| README.md | 2 | 双方各自补充说明 |
| 其余 10 个文件（Token.cpp/ExpressionEvaluator/InsertExecutor/UpsertExecutor/TransactionManager/IndexAccessPath/BPlusTree.h/ExecutionEngine.h/Optimizer.h/BufferPoolManager.h/TableHeap.h） | 各 1 | 单点冲突，取双方并集即可 |
| `src/codegen/CodeGenerator.cpp` | modify/delete | **main 已删除 codegen，本分支保留**——合并时按 main 删除（codegen 已被 main 的 StorageAccess/TimingProxy 替代） |

## 4. main 分支关键架构变更（合并时必须对齐）

1. **删除 codegen**：`include/codegen/CodeGenerator.h`、`src/codegen/CodeGenerator.cpp` 被删，新增 `include/storage/StorageAccess.h`、`src/storage/StorageAccess.cpp`（统一存储访问门面）、`TimingProxyExecutor`（EXPLAIN ANALYZE 计时）、`SemanticErrorStage`（语义错误分级）。
2. **类型强制转换重构**：新增 `include/execution/TypeCoercion.h`，Insert/Update/Delete/Upsert/Aggregate/Window/ExpressionEvaluator 全面接入；合并后本分支的写算子改动须适配 TypeCoercion 接口。
3. **Token/Lexer 调整**：`Token.h/Token.cpp/Lexer.cpp` 双方均有改动（main 加关键字与类型，本分支加 `KEYWORD_SNAPSHOT`）——需并集保留。
4. **测试体系重构**：SQL 测试重编号为 52-91（三/两位序），删除 07_alias.sql、err_* 测试，新增 run_tokens_complete/run_debug_meta/run_plan_optimized.bat；本分支的 00-53 SQL 回归脚本编号需映射对齐，`tests/sql/53_subquery_decorrelate.sql`（本分支新增）需与新编号体系协调。
5. **事务/存储引擎**：TransactionManager、TableHeap 有 MVCC 模式与唯一约束改动；`70_mv_schema.sql` 覆盖本分支快照/版本链区域的潜在语义差异。

## 5. 本分支待并入的独特成果（main 不具备）

- Phase5：温度感知刷盘 + 自适应阈值、锁分片（16 shard）、谓词区间树增量更新、组提交时间窗、页 CRC 介质监测、`\bench` 进程内基准、`\stats`/`\analyze` 可观测性扩展。
- Phase6 U1b/c/d：B+Tree 内节点合并、根收缩、物理删页 + `TryFreePage` 延迟回收、游标重下降。
- Phase6 U2：锁等待分布观测、组提交调参曲线（推荐 10ms）。
- Phase6 U3：JOIN 顺序启发式（`ReorderJoins`）、聚合下推（`PreAggScan`）、子查询去关联（`DecorrelateSubqueries` → SEMI/ANTI JOIN）+ 子查询物化缓存 + 递归 CTE 缓存失效。
- 存储层单元测试 `tests/storage/storage_ut.cpp`（292643 checks）——main 无此文件，合并时按新增保留。

## 6. 合并执行预案（展示后执行）

```text
步骤 0  前提：展示完成，本地基线全绿（storage_ut 292643/0fails，SQL 56/0）
步骤 1  git fetch origin && git switch os-page-management
步骤 2  git merge origin/main           # 预期 28 文件冲突
步骤 3  按 §3 冲突清单逐文件解决：
        - 代码生成类：接受 main 删除（codegen 已被替代）
        - 单点冲突（≤2 hunk）：取双方并集，保留本分支 Phase5/6 特性
        - TableHeap.cpp（22 hunk）：以 main 的 TypeCoercion/MVCC 模式为基底，
          逐块回嵌本分支快照版本链/索引墓碑/内联真空改动
        - ExecutionEngine/Executor.h：双方执行器注册表并集（PRE_AGG_SCAN 保留）
        - Optimizer.cpp：main 的 IndexAccessPath 重构为基底，
          回嵌 ReorderJoins/PushDownAggregates/DecorrelateSubqueries 三个 pass
        - Token/Lexer：关键字并集（含 KEYWORD_SNAPSHOT）
        - tests/sql：以 main 52-91 编号为基底，映射本分支回归用例，
          新增 53_subquery_decorrelate.sql 并入
        - storage_ut.cpp：main 无此文件，git add 保留本分支版本
步骤 4  构建：cmake -S . -B build && cmake --build build -j
步骤 5  回归：tests\run_repro_all.ps1（storage_ut + SQL 回归 + 参数扫描）
        以及 main 侧 run_all_tests.bat / run_tokens_complete.bat 等
步骤 6  修复回归问题直至全绿（含 -Wall -Wextra 零告警）
步骤 7  git add -A && git commit（信息注明 merge main 与解决项）
         git push origin os-page-management
步骤 8  证据存档 docs/test_evidence/merge_main_<date>.log
```

**回退保险**：任意步骤发现不可控回归，立即 `git merge --abort` 回到 e45f1fc 基线，评估后重试。

## 7. 其他分支处理

- `origin/cheng`（9d3e02b）、`origin/ckm`（7a4bf56）：与本模块无重叠代码，未纳入本次同步范围；如需并入在 main 合并完成后另行评估。
- `origin/feature/codegen-5and7`（e33782d）：codegen 已被 main 移除，此线不再需要合并。

---

## 8. 后续执行记录：`main` 并入本分支（2026-09-16）

预制预案 §6 的「演示后深度合并」已执行完毕，结果与预判一致（main 已含引擎层架构重构），本分支现已完成合并：

| 事项 | 状态 | 说明 |
|---|---|---|
| 拉取 `origin/main` 最新 | ✅ 完成 | `git fetch origin main` → 本地 `main` = FTCH_HEAD = `1a1c989`（behind 0 / ahead 18） |
| 本分支并入 main | ✅ 完成 | `os-page-management` 按 **快进（fast-forward）** 对齐到 `main`：`165cda1 → 1a1c989`。核对提交拓扑后确认本分支为 `main` 的**纯子集**（`rev-list --count main...os-page-management = 48 0`，本分支无独有提交），故快进无损、可完全回退 |
| OS 模块功能保留 | ✅ 完成 | `include/storage/OsModuleOptimizations.h` + `src/storage/OsModuleOptimizations.cpp` 及 `docs/os_module_archive` 全量保留于合并后树 |
| 回归验证（在 `os-page-management` 分支上执行） | ✅ 全绿 | `storage_ut` **292641 checks / 0 fails PASS**；`tests\run_sql_regression.ps1` **PASSED=88 FAILED=0**；`verify_outputs.py` **84/84 PASS** |

> 结论：本分支现与 `main` 指向同一提交（`1a1c989`），此前因分支落后 48 个提交（缺失近期存储线修复）导致的测试全部失败问题已消除，两分支功能完全兼容。
