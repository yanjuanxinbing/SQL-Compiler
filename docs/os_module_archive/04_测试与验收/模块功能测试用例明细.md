# 操作系统页面管理模块 · 功能测试用例明细

- 项目：SQL-Compiler 小型数据库系统（编译原理 + 操作系统 + 数据库 三模块综合实训）
- 模块：**操作系统页面管理**（页式存储 / 缓冲池 / 替换策略 / 持久化 / 事务并发与崩溃恢复 / 索引与查询优化）
- 编制日期：2026-09-14（收官版）
- 依据：指导书三.2「操作系统页面管理」+ 扩展开发 E1–E8 + OS 优化 Phase 1–4 + Phase 6 U1–U3
- 用例分层：S=storage_ut 确定性断言；R=SQL 回归脚本；M=手动 CLI 冒烟；C=崩溃注入两阶段
- 执行记录与证据：`docs/test_evidence/final_verify.log`、`sql_regression_run.log`、`storage_ut.log`、各阶段验收日志

> 说明：下表「实际结果」为 2026-09-14 收官全量验证实测值；每行可独立复现（命令见「输入数据」列）。

---

## 一、页式存储与空闲页管理

| 编号 | 功能点 | 测试目的 | 输入数据（命令/脚本） | 预期输出 | 实际结果 | 结论 |
|---|---|---|---|---|---|---|
| S1.1 | 页分配/释放/读回 | 验证 4KB 页、页号唯一、读回一致 | storage_ut 页分配/释放/读回断言组 | 页号单调递增、写后读回一致、释放后可复用 | ✅ 292643 checks 全过 | ✅ |
| S1.2 | 空闲页跨重启持久化（.fpl） | 释放页在重启后仍可复用，防活页误复用 | storage_ut `.fpl` 重启复用/防误复用/旧库兼容/页数漂移丢弃 | 位图装载一致、活页绝不复用、漂移时安全丢弃 | ✅ 全过 | ✅ |
| S1.3 | 页 CRC 损坏检测（.crc） | 写盘记录校验、读回核对、坏块拦截 | storage_ut 页 CRC 写入/核对/回收清除 | 校验和一致、坏块被拦截报错、0=无记录兼容旧库 | ✅ 全过 | ✅ |
| S1.4 | 64 位文件定位 | >2GB 文件偏移正确 | storage_ut `_fseeki64/_ftelli64` 路径 | 大偏移读写不溢出 | ✅ 全过 | ✅ |
| S1.5 | 页内分配器 | first-fit 分配/合并/碎片统计/边界防御 | storage_ut 页内分配器断言组 | 分配紧凑、相邻合并、边界不越界 | ✅ 全过 | ✅ |

## 二、缓存管理与替换策略

| 编号 | 功能点 | 测试目的 | 输入数据 | 预期输出 | 实际结果 | 结论 |
|---|---|---|---|---|---|---|
| S2.1 | LRU 替换 | 最久未用优先淘汰，O(1) 双链表+定位器 | storage_ut LRU 断言 + 吞吐基准 | 淘汰序正确；2.4M ops/s（1.08× 优化前） | ✅ 全过 | ✅ |
| S2.2 | FIFO / CLOCK 替换 | 先进先出 / 二次机会参考位 | storage_ut FIFO/CLOCK 断言 | 淘汰序与算法一致 | ✅ 全过 | ✅ |
| S2.3 | LRU-K 替换 | 双集合结构（K=1 退化为 LRU），命中率对照 | storage_ut LRU-K vs LRU 命中率对照 | LRU-K 命中率 ≥ LRU，K=1 ≡ LRU | ✅ 全过 | ✅ |
| S2.4 | 命中统计与替换日志 | `\stats` 输出命中率、替换日志、IO 计数 | CLI `\stats` | 命中/缺失/替换计数与日志 1024 条环形 | ✅ 输出正确 | ✅ |
| S2.5 | 内存上限可配置 | `SQLCOMPILER_BUFFER_MEMORY` 换算帧数 | `SQLCOMPILER_BUFFER_MEMORY=8192` 启动 + `\stats` | 缓冲池降为 2 帧、已用/上限显示 | ✅ 输出正确 | ✅ |
| S2.6 | 页级读写闩 | 防写盘撕裂、多读并发、写写互斥 | storage_ut 页闩并发断言 | 读写不互斥、写写互斥、落盘前持读锁 | ✅ 全过 | ✅ |
| S2.7 | 后台刷脏 | 周期刷脏不调 Sync、可停止 | `SQLCOMPILER_BG_FLUSH_MS=50` + storage_ut | ticks 递增、写回直方图、退出无悬挂 | ✅ 全过（t4_bg_flush_console.log） | ✅ |
| S2.8 | 温度感知刷盘 | 只写回冷脏页、热页留池 | storage_ut 温度自适应 + 参数扫描 | 冷页写回、热页保留、自适应阈值 | ✅ 全过（param_sweep.log） | ✅ |

## 三、磁盘 I/O 与块设备

| 编号 | 功能点 | 测试目的 | 输入数据 | 预期输出 | 实际结果 | 结论 |
|---|---|---|---|---|---|---|
| S3.1 | IO 读写计数 | `\stats` IO 统计准确 | storage_ut io_read/write 计数 | 计数与操作一致 | ✅ 全过 | ✅ |
| S3.2 | 块设备故障注入 | 写失败/读失败/坏块拦截 | `FaultInjectingBlockDevice` | 失败路径被正确上报，不产生撕裂数据 | ✅ 全过 | ✅ |
| S3.3 | 介质多样性 | 内存/稀疏文件/回环网络块设备 | storage_ut 介质断言（T4） | 三种介质行为一致 | ✅ 全过 | ✅ |

## 四、日志与崩溃恢复

| 编号 | 功能点 | 测试目的 | 输入数据 | 预期输出 | 实际结果 | 结论 |
|---|---|---|---|---|---|---|
| C4.1 | WAL-before-data | 数据页落盘前日志已持久 | 崩溃注入 49 两阶段 | 未提交 UPDATE 回滚、已提交持久 | ✅ Phase1 exit=1 → Phase2 语义正确 | ✅ |
| C4.2 | redo / undo / CLR 链 | 崩溃中断后持续回滚 | 崩溃注入 50 两阶段（`\crash_after_undo_steps 1`） | 仅 3 行、4/5/6 由 CLR 补做 undo | ✅ Phase1 exit=1 → Phase2 仅 3 行 | ✅ |
| S4.3 | WAL 组提交 | 并发提交合并 fsync | storage_ut 组提交基准 + 窗口参数扫描 | 128 提交 = 12~13 次 fsync（9.8~10.7× 削减） | ✅ gc_sweep.log 曲线 | ✅ |
| S4.4 | 真持久 fsync | `_commit(_fileno)` 等价 POSIX fsync | storage_ut 持久性断言 | 掉电后数据不丢 | ✅ 全过 | ✅ |

## 五、并发与事务隔离

| 编号 | 功能点 | 测试目的 | 输入数据 | 预期输出 | 实际结果 | 结论 |
|---|---|---|---|---|---|---|
| S5.1 | 多会话并发 + 死锁检测 | 并发安全、等待图 DFS 检测环并中止 victim | storage_ut 多会话并发断言 | 无死锁、victim 中止且无残留等待边 | ✅ 全过 | ✅ |
| S5.2 | 隔离级别 | RC/RR/SERIALIZABLE/SNAPSHOT 语义 | storage_ut 隔离断言 + 52_set_isolation | 读锁语句末释放/提交释放；SNAPSHOT 免读锁 | ✅ 全过 + SQL 52 通过 | ✅ |
| S5.3 | 行级锁 + 多粒度 | 表锁/行锁层级冲突、行锁升级 | storage_ut 锁升级/多粒度断言 | 冲突判定正确、阈值自适应 | ✅ 全过 | ✅ |
| S5.4 | 谓词锁区间树 | (表,列,区间) 冲突 O(log P+K) | storage_ut 谓词锁断言 | 写前精确匹配、防幻读、增量更新 | ✅ 全过 | ✅ |
| S5.5 | MVCC 快照 + FCW | 版本链可见性、丢失更新提交期拦截 | storage_ut MVCC/FCW 断言 | 快照读正确、FCW 输家中止 | ✅ 全过 | ✅ |
| S5.6 | 二级索引 MVCC 可见性 | 索引回表版本重检 | storage_ut 索引快照扫描断言 | 不读已删版本、键区间正确 | ✅ 全过 | ✅ |
| S5.7 | 快照低水位 O(1) | 活动快照最小 CSN 缓存 | storage_ut 低水位基准 | 10k 读取 157µs（≈465× 旧实现） | ✅ 全过 | ✅ |

## 六、索引（B+Tree）

| 编号 | 功能点 | 测试目的 | 输入数据 | 预期输出 | 实际结果 | 结论 |
|---|---|---|---|---|---|---|
| S6.1 | 乐观页级并发 | 读不阻塞写、无锁等待 | storage_ut 乐观并发 + 4/8/16 线程基准 | 3.18×（16 线程 vs 树级锁） | ✅ 全过 | ✅ |
| S6.2 | 分裂/合并/再平衡 | 慢路径预分裂、删后重平衡（<40% 合并） | storage_ut U1/U1b 断言 | 结构合法、利用率恢复、父分隔键更新 | ✅ 全过（U1d 校验短路） | ✅ |
| S6.3 | 物理删页与可靠回收 | TryFreePage + 待回收队列 | storage_ut U1c 断言 | 页进 .fpl、并发 pin 延迟回收、去重 | ✅ 全过 | ✅ |
| S6.4 | 索引墓碑回收 | DecideIndexEntry + 索引 Vacuum | storage_ut 墓碑回收断言 | 死键物理删除、活动快照可见保留 | ✅ 全过 | ✅ |

## 七、查询优化（Phase 6 U3，集成验证）

| 编号 | 功能点 | 测试目的 | 输入数据 | 预期输出 | 实际结果 | 结论 |
|---|---|---|---|---|---|---|
| R7.1 | JOIN 顺序启发式 | 全 INNER 左深链小表驱动重排 | TestJoinReorder + EXPLAIN | 重排后基数组装次序、OUTER 守底零回归 | ✅ 全过（join_reorder_u3_1.log） | ✅ |
| R7.2 | 聚合下推 | 单表裸 COUNT/SUM 扫描内预聚合 | TestPreAggPushDown 52 项 | EXPLAIN 含 PreAggScan、语义等价、零改写守底 | ✅ 全过（preagg_pushdown_u3_2.log） | ✅ |
| R7.3 | 子查询去关联 | 相关 EXISTS/IN/ANY → SEMI/ANTI JOIN | TestSubqueryDecorrelation 10 场景 | EXPLAIN Join(SEMI/ANTI)、NOT IN 守底、结果一致 | ✅ 全过（subquery_decorrelate_u3_3.log） | ✅ |
| R7.4 | 非相关子查询物化 | 每语句一次、外层行命中 | TestSubqueryMaterialization 白盒计数 | materialize=1/hits=4（5 外层行）、相关不入缓存 | ✅ 全过 + `\stats` 同源 | ✅ |
| R7.5 | 递归 CTE 缓存失效 | 迭代边界清缓存 | TestRecursiveCteCacheInvalidation | 序列 1,2,4,8（cnt=4, mx=8）、materialize=3 | ✅ 全过 | ✅ |

## 八、端到端 SQL 回归与可观测性

| 编号 | 功能点 | 测试目的 | 输入数据 | 预期输出 | 实际结果 | 结论 |
|---|---|---|---|---|---|---|
| R8.1 | 全量 SQL 回归 | 编译器+OS+DB 三模块集成正确 | `tests\run_sql_regression.ps1`（54 条脚本 + 2 套崩溃注入） | 全部 exit 0、无 `Error:` 行 | ✅ **56 passed / 0 failed** | ✅ |
| R8.2 | 崩溃注入两阶段 | 49/50 恢复语义 | 49_acid_recovery / 50_undo_clr | Phase1 非 0、Phase2 语义正确 | ✅ 见 C4.1/C4.2 | ✅ |
| M8.3 | `\stats` 观测面 | 命中/IO/锁等待/索引/子查询缓存等全指标 | CLI `\stats` | 各段指标完整且与操作一致 | ✅ 输出正确 | ✅ |
| M8.4 | `\analyze` / `\bench` | 页面/索引分析、并发压力 | CLI `\analyze`、`\bench 8 300` | 分析输出完整、全 ok=2400 err=0 | ✅ 输出正确 | ✅ |
| S8.5 | 告警审计 | `-Wall -Wextra` 零告警 | 全量重建 | 0 warning / 0 error | ✅ 0/0 | ✅ |

---

## 汇总

| 层 | 结果 |
|---|---|
| 单元测试（storage_ut） | **292643 checks / 0 fails / RESULT: PASS** |
| SQL 回归（含崩溃注入） | **56 passed / 0 failed** |
| 构建审计 | **0 错误 / 0 告警**（`-Wall -Wextra`） |
| 性能证据 | LRU-K 命中率 ≥ LRU；B+Tree 乐观并发 3.18×；低水位 465×；组提交 fsync 9.8× 削减；CRC 283 MB/s |

> 复现：`powershell -ExecutionPolicy Bypass -File tests\run_repro_all.ps1`（构建→UT→SQL 回归→参数扫描→图表，ALL GREEN）。
