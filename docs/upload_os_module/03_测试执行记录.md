# 操作系统页面管理模块 · 功能测试执行记录

- 项目：SQL-Compiler 小型数据库系统
- 模块：操作系统页面管理（页式存储 / 缓存管理 / 持久化 / 并发与恢复）
- 测试日期：2026-09-14（T5 收口最终版；历史执行记录见 `docs/test_evidence/`）
- 测试依据：`docs/upload_os_module/02_功能测试方案.md`
- 测试环境：Windows（PowerShell 5.1）、CMake + MinGW GCC（`D:/mingw64/bin/gcc.exe`）、C++17 Debug 构建
- 构建产物：`build/sqlcompiler.exe`、`build/storage_ut.exe`

---

## 1. 测试执行总览

| 测试层 | 执行命令 | 结果 |
|---|---|---|
| 存储单元测试 | `build\storage_ut.exe` | **58953 checks / 0 fails，RESULT: PASS** |
| 全量 SQL 回归 | `tests\run_sql_regression.ps1`（53 条脚本 + 2 套崩溃注入） | **55 passed / 0 failed**（耗时 4.50 s） |
| 崩溃恢复注入 | `49_acid_recovery`（两阶段） | Phase1 exit=1（崩溃生效）→ Phase2 exit=0，语义正确 |
| CLR 链崩溃恢复 | `50_undo_clr`（两阶段） | Phase1 exit=1（撤销中途崩溃）→ Phase2 exit=0，语义正确 |
| 告警审计 | `-Wall -Wextra` 全量编译 | **0 warning / 0 error**（`docs/test_evidence/audit_build.log`） |

> 复现方法：重新执行 `cmake --build build --config Debug` 后，运行
> `powershell -ExecutionPolicy Bypass -File tests\run_sql_regression.ps1`
> 与 `Set-Location build; .\storage_ut.exe`，结果可复现。

---

## 2. 存储单元测试数据（storage_ut.exe）

### 2.1 结果摘要

```text
======== Storage UT ========
checks: 58953   fails: 0
RESULT: PASS
```

覆盖类别：页式存储（页分配/释放/读回、`.fpl` 空闲页跨重启持久化）、替换策略（LRU/FIFO/CLOCK/LRU-K 及命中率对照）、缓冲池（内存上限、页级读写闩、后台刷脏、温度感知刷盘）、磁盘 I/O（故障注入、页 CRC）、页内分配器、WAL 与组提交、undo/redo/CLR、多会话并发、死锁检测、隔离级别（RC/RR/SERIALIZABLE/SNAPSHOT）、谓词锁、行锁升级、MVCC 版本链、FCW、二级索引快照扫描、乐观 B+Tree 分裂/扫描、谓词区间树、快照低水位 O(1)、内联真空、索引墓碑回收、后台真空线程；**T4 介质与可观测性**（内存/稀疏/回环网络块设备、命中构成分档、脏页年龄分布、后台刷脏直方图、页映射快照、CRC 累计计数、`\stats`/`\analyze` 输出）。

### 2.2 OS 优化性能基准（随 storage_ut 一并输出）

| 基准项 | 优化后 | 基线（优化前） | 收益 |
|---|---|---|---|
| Stage1 CRC 吞吐 | 283.0 MB/s | 281.5 MB/s | 一致（收益在消除惰性建表竞态 / 编译期表中初始化） |
| Stage2 LRU 吞吐 | 2.4 M ops/s | 2.3 M ops/s | 加速 1.08× |
| Stage3 页分配遍历 | 64.0 次访问/次分配 | 128.0 次 | 减少 50.0% |
| 快照低水位读取 | 10k 次读取 157 µs（重算 0 次） | 旧实现 10k 次扫描 73005 µs | 约 **465×** |
| WAL 组提交 | 128 提交 = 13 次并发 fsync | 128 次串行 fsync | **9.8× 减少 fsync 系统调用** |

---

## 3. SQL 回归数据（sqlcompiler.exe + tests/sql）

### 3.1 执行方式

`tests/run_sql_regression.ps1` 以 `System.Diagnostics.Process` 启动 `sqlcompiler.exe`，UTF-8 字节级逐行注入 stdin，逐脚本独立建库，判定规则：退出码 0 且输出无 `sqlcompiler> Error:` 行。

### 3.2 结果

```text
PASSED=55 FAILED=0 ELAPSED=4.6168108s
```

通过清单（53 条 SQL + 2 套崩溃注入）：`00_smoke` ~ `52_set_isolation` 全部通过；
`49_acid_recovery`、`50_undo_clr` 两阶段崩溃注入通过。

### 3.3 崩溃注入关键数据

**49_acid_recovery**（崩溃发生在「未提交 UPDATE」之后）：

```text
[phase1 exit=1]        ← \crash 使进程非 0 退出，崩溃生效
Phase2 首查：id=1 bal=100  id=2 bal=50   ← 未提交的 UPDATE(999) 被 WAL undo 回滚
Phase2 末查：id=1 bal=100  id=2 bal=60   ← 已提交的 UPDATE(+10) 持久化；ROLLBACK 生效
```

**50_undo_clr**（崩溃发生在 ROLLBACK 撤销第 1 步之后）：

```text
[phase1 exit=1]        ← \crash_after_undo_steps 1 使进程在撤销中途崩溃
[crash-after-undo-steps] triggering _Exit(1) after undo step
Phase2 查询：仅 3 行（1|100, 2|200, 3|300）  ← 4/5/6 行的插入由 CLR 链补做完整 undo
```

两套用例验证了：WAL-before-data、redo 重放、undo 回滚、CLR 链在崩溃中断后继续完成撤销、已提交数据持久性。

---

## 4. 测试中发现的问题与解决方案

### 4.1 问题 A：PowerShell/.NET 管道注入 UTF-8 BOM，首条语句词法错误（阻塞性问题）

- **现象**：崩溃注入两阶段用例（49/50）在脚本化运行时偶发/持续报
  `Error: [Lexical] line=1, col=1: unexpected character: '�'`，随后连锁出现
  `table not found: acct` 语义错误；手工在终端输入或 `cmd` 文件重定向 `(`<``)` 均正常。
- **定位过程**：
  1. 用 `diag_run.ps1` 逐行打印**写入** stdin 的字节，确认写入内容为干净 ASCII
     `43 52 45 41 54 ...`（无 BOM），排除脚本编码问题；
  2. 对比 `Get-Content -Raw | & exe` 与 `.NET Process.StandardInput` 两种喂入方式，
     并试验 CRLF/LF、单块/逐行、StreamWriter/BaseStream 全部组合；
  3. 临时在 `src/main.cpp` 增加逐行 hex dump 插桩，复现后证实**进程实际读到的首行**
     前缀为 `EF BB BF`（UTF-8 BOM）——即 Windows PowerShell/.NET 向子进程管道写入
     首批输入时自动附加了 BOM 前导（`.NET Framework StreamWriter` 使用带 BOM 的
     `UTF8Encoding`），而写入方日志显示的是清洗后的字节。
- **根因**：编译器 REPL 未处理输入流首部 BOM；BOM 首个字节 `0xEF` 被词法器判为
  「非法字符」，首条语句失败 → 建表未执行 → 后续语句全部连锁失败 → Phase2 无表可恢复。
- **解决方案**（代码级修复，见 §4.1 修复清单）：
  1. `src/main.cpp`：`std::getline` 读入每行后，若行首为 `EF BB BF` 则剔除（BOM 只可能
     出现在输入开头，清除后不影响任何合法 SQL）；该修复同时惠及「用户重定向 Notepad
     保存的带 BOM 脚本」等真实场景，属于 CLI 健壮性增强；
  2. `tests/run_sql_regression.ps1`：执行器路径由陈旧的 `build\Debug\sqlcompiler.exe`
     更正为当前构建产物 `build\sqlcompiler.exe`（MinGW 单配置构建输出在 `build\` 根）；
  3. 同步更正 `tests/run_all_tests.bat`、`tests/run_acid_recovery.bat`、
     `tests/run_acid_clr.bat` 的 `BUILD_DIR` 为 `%ROOT_DIR%\build`。
- **验证**：修复后 49/50 两阶段用例连续多轮通过（PASSED=55 FAILED=0），
  `storage_ut` 58325 checks / 0 fails 不受影响。

### 4.2 问题 B：单元测试输出残留调试打印，污染测试证据

- **现象**：`storage_ut.exe` 输出中出现 `[DBG-rb] txn=... undo page=...`、
  `[DBG-del] ...` 等临时调试行。
- **解决方案**：删除 `src/txn/TransactionManager.cpp` Rollback 循环内的无条件
  `std::cerr << "[DBG-rb]..."` 与 `tests/storage/storage_ut.cpp`
  TestSnapshotDeleteFcw 内的 `[DBG-del]`/`[DBG-del-idx]` 打印（断言 CHECK 保留）。
- **验证**：重建后输出干净，仅保留有意设计的基准输出（`[low-water]`、`[Phase4] group-commit`、`==== OS 模块性能对比基准 ====`）。

### 4.3 问题 C：构建产物路径不一致

- **现象**：`build\Debug\` 下存在旧版 `sqlcompiler.exe`（与当前源码不一致），
  多个测试脚本引用该陈旧路径。
- **根因**：工程 CMake 生成器为单配置 **MinGW Makefiles**（`CMAKE_BUILD_TYPE=Debug`），
  产物直接输出到 `build\` 根目录；`build\Debug\` 是历史遗留的陈旧产物。
- **解决方案**：统一测试脚本指向 `build\sqlcompiler.exe`（见 §4.1）。

---

## 5. 通过准则达成情况

| 准则 | 目标 | 实际 | 结论 |
|---|---|---|---|
| storage_ut 无失败 | 0 fails | 58953 checks / 0 fails | ✅ |
| SQL 回归 | 55 passed / 0 failed | 55 / 0（含崩溃注入） | ✅ |
| 崩溃注入两阶段 | Phase1 非 0、Phase2 exit 0 且语义正确 | 49：1→0；50：1→0，恢复语义正确 | ✅ |
| 稳定性 | 无死锁/超时/flaky | 并发用例多轮复跑稳定 | ✅ |

## 6. 测试证据文件

| 文件 | 内容 |
|---|---|
| `docs/test_evidence/storage_ut_run.log` | 存储单元测试完整输出（含性能基准） |
| `docs/test_evidence/sql_regression_run.log` | 55 条用例逐条 [OK] 日志 |
| `docs/test_evidence/audit_build.log` | `-Wall -Wextra` 全量编译日志（0 warning / 0 error） |
| `docs/test_evidence/t4_obs_diag_console.log` | `\stats` + `\analyze` 全指标输出（命中构成/脏页年龄/刷脏直方图/页映射） |
| `docs/test_evidence/t4_bg_flush_console.log` | 后台刷脏运行日志（ticks 与直方图记账） |
| `tests/tmp/49_acid_recovery.out` | 49 崩溃恢复两阶段完整输出 |
| `tests/tmp/50_undo_clr.out` | 50 CLR 链两阶段完整输出 |

## 7. T5 收口审计记录（2026-09-14）

- **告警审计**：`-Wall -Wextra` 全量编译 **0 warning / 0 error**（`build_audit/`）。清理内容：
  - 8 处 switch 补 `default`（Token 关键字枚举、语句类型、连接类型、字面量类型等，消除 `-Wswitch` 208 条）；
  - 删除 5 个未使用匿名命名空间函数、2 个未使用变量、2 个未使用参数（`-Wunused-*`）；
  - 修复 3 处有符号/无符号比较（`-Wsign-compare`）、1 处无符号恒真比较（`-Wtype-limits`）、
    DiskManager 构造初始化序（`-Wreorder`）；
  - MSVC 专用 `#pragma warning` / `#pragma comment` 加 `_MSC_VER` 防护（MinGW 不识别）。
- **回归复核**：清理后 storage_ut **58953 checks / 0 fails**、SQL 回归 **55 passed / 0 failed**，与清理前基线一致，行为零变化。
- **最终交付**：README、验收报告、需求比对文档均已同步到 T5 基线。

## 8. Phase 6 U3-2 测试执行增补（2026-09-14）

### 8.1 执行总览（U3-2 聚合下推）

| 测试层 | 执行命令 | 结果 |
|---|---|---|
| 构建 | `cmake -S . -B build`（重新拾取 `PreAggScanExecutor.cpp`）+ `cmake --build build -j 8` | **0 错误 / 0 告警**（`-Wall -Wextra`） |
| 存储单元测试 | `build\storage_ut.exe` | **292554 checks / 0 fails，RESULT: PASS**（相对 U3-1 基线 292502，新增 `TestPreAggPushDown` 52 项断言） |
| 全量 SQL 回归 | `tests\run_sql_regression.ps1` | **55 passed / 0 failed**（ELAPSED=4.6281447s） |
| CLI 冒烟（EXPLAIN + 结果一致性） | `sqlcompiler.exe` 逐条注入 | 合格改写 / 语义等价 / 零改写守底 / 空表边界全部正确 |

### 8.2 测试方法与关键指令

1. 单测：`TestPreAggPushDown` 走 Database→Planner→Optimizer→执行全链路，断言分四组——
   (1) 合格形态改写（EXPLAIN 含 `PreAggScan`）；(2) 语义等价（COUNT(*)/COUNT(col)/SUM/分组/ORDER BY）；
   (3) 不合格形态零改写（AVG/COUNT(DISTINCT)/COALESCE 包装/JOIN 聚合，EXPLAIN 不含 `PreAggScan` 且结果正确）；
   (4) 空表 `COUNT(*)=0`、`SUM(x)=NULL`。
2. 回归：`powershell -ExecutionPolicy Bypass -File tests\run_sql_regression.ps1`。
3. 冒烟：`SELECT grp, COUNT(*), SUM(val) FROM t1 GROUP BY grp` EXPLAIN 输出
   `Project(grp, COUNT(1), SUM(val)) → PreAggScan(...) → SeqScan(t1)`，结果 `10|2|300 / 20|1|NULL / 30|1|300`。

### 8.3 问题记录

| 现象 | 根因 | 处理 |
|---|---|---|
| `ExecutionEngine` 链接缺 PreAggScanExecutor | BuildExecutor 无 PRE_AGG_SCAN 分支、头文件未包含 | 补 `#include "execution/PreAggScanExecutor.h"` 与 `case PlanNodeType::PRE_AGG_SCAN` 分支 |
| 集成冒烟发现 `COUNT(*)` 被归一化为 `COUNT(1)` | 解析器字面量归一化，非缺陷 | 语义与 AggregateExecutor 一致（`COUNT(1)`≡`COUNT(*)` 计行），实测 4 正确，无需改码 |

### 8.4 证据

- `docs/test_evidence/preagg_pushdown_u3_2.log`：U3-2 验收证据（构建/UT/SQL/EXPLAIN 实证/实现要点/方案对比）。
- 详细设计与备选方案对比见 `11_Phase6_U3_查询优化增强_阶段文档.md`。

## 9. Phase 6 U3-3 测试执行增补（2026-09-14）

### 9.1 执行总览（U3-3 子查询去关联）

| 测试层 | 执行命令 | 结果 |
|---|---|---|
| 构建 | `cmake --build build -j 8`（强制重编译 Optimizer/Execution/Join/CTE 相关 TU） | **0 错误 / 0 告警**（`-Wall -Wextra`） |
| 存储单元测试 | `build\storage_ut.exe` | **292643 checks / 0 fails，RESULT: PASS**（相对 U3-2 基线 292554，新增 `TestSubqueryDecorrelation` 10 场景 + `TestSubqueryMaterialization` + `TestRecursiveCteCacheInvalidation`） |
| 全量 SQL 回归 | `tests\run_sql_regression.ps1` | **56 passed / 0 failed**（新增 `53_subquery_decorrelate.sql` 13 项用例 + `\stats` 证据） |
| CLI 冒烟（EXPLAIN + 结果一致性） | 逐条注入 | `Join(SEMI/ANTI, ...)` 改写成立、NOT IN 守底保持 Filter、嵌套两层递归去关联、递归 CTE 序列 1,2,4,8 |
| 白盒计数 | `\stats` 同源 sink | 非相关子查询 materialize=1/hits=4；递归 CTE 3 次迭代重物化（materialize=3） |

### 9.2 测试方法与关键指令

1. `TestSubqueryDecorrelation`：EXPLAIN 断言 + 结果断言相互印证，覆盖相关 EXISTS→SEMI / NOT EXISTS→ANTI / IN→SEMI / ANY→SEMI / 混合合取左子保留 Filter / NOT IN 保守不改写 / 非相关 EXISTS 无条件 / 空表 / NULL 语义 / 嵌套两层。
2. `TestSubqueryMaterialization`：4 步白盒计数（非相关 1 物化 4 命中；相关与未限定列子查询不入缓存）。
3. `TestRecursiveCteCacheInvalidation`：`WITH RECURSIVE ... SELECT n + (SELECT max(acc.n) FROM acc)` → cnt=4/mx=8 + materialize=3。
4. 回归：`powershell -ExecutionPolicy Bypass -File tests\run_sql_regression.ps1`。

### 9.3 问题记录

| 现象 | 根因 | 处理 |
|---|---|---|
| 会话路径子查询计数不累计 | Database.cpp 会话路径 `session_ctx` 未注入统计 sink | 补 `&subquery_cache_stats_` 第三参 |
| 递归 CTE 子查询因未限定列不入缓存 | `WalkExprForOuterRefs` 保守视未限定列为外层引用（设计取舍） | 测试改用限定列 `acc.n`，并补断言记录保守行为 |
| storage_ut 首跑偶发失败 | `TestOptimisticSplitConcurrency` 已知 flaky（重跑通过，与本次变更无关） | 复跑确认 |

### 9.4 证据

- `docs/test_evidence/subquery_decorrelate_u3_3.log`：U3-3 验收证据。

## 10. 收官全量验证（2026-09-14，任务 1「功能测试与冲突验证」）

### 10.1 执行总览

| 测试层 | 执行命令 | 结果 |
|---|---|---|
| 全量构建 | `cmake --build build -j 8` | **0 错误 / 0 告警**（`-Wall -Wextra`） |
| 存储单元测试 | `build\storage_ut.exe` | **292643 checks / 0 fails，RESULT: PASS** |
| 全量 SQL 回归 | `tests\run_sql_regression.ps1`（54 条脚本 + 2 套崩溃注入） | **56 passed / 0 failed**（ELAPSED=5.05s） |
| 崩溃恢复注入 | 49_acid_recovery / 50_undo_clr 两阶段 | Phase1 非 0 → Phase2 语义正确 |
| 边界/异常冒烟 | `SQLCOMPILER_BUFFER_MEMORY=8192` + 错误路径 + `\stats`/`\analyze` | 2 帧生效、错误优雅报告不崩溃、观测指标完整 |
| 集成冲突验证 | 编译器+OS+DB 三模块端到端（54 条 SQL） | 无冲突，全部通过 |

### 10.2 边界/异常场景抽查（补充）

| 场景 | 输入 | 实测 |
|---|---|---|
| 缓冲池内存上限 | `SQLCOMPILER_BUFFER_MEMORY=8192` | `buffer pool frames: 2`、`8192 B / 8192 B` ✅ |
| 表不存在 | `SELECT * FROM no_such_table` | `Error: semantic error: table not found`（不崩溃，CLI 继续） ✅ |
| 重复建表 | `CREATE TABLE t(...)` 两次 | `Error: semantic error: table already exists` ✅ |
| 主键冲突 | `INSERT` 重复 id | 错误报告，不崩溃 ✅ |
| 磁盘校验 | `.crc` 全程 | `crc errors: 0` ✅ |

### 10.3 证据

- `docs/test_evidence/final_verify.log`：收官全量验证完整日志（构建 + UT + SQL 回归）。
- `tests/tmp/boundary_smoke.out`：边界/异常冒烟输出。
- 明细见 `12_模块功能测试用例明细.md`（每功能点：目的/输入/预期/实际/结论）。
