# U3-2 聚合下推（Aggregate → PreAggScan）· 验收证据
- 项目：SQL-Compiler（编译原理 + 操作系统 + 数据库 三模块综合实训）
- 模块：编译/执行层（编译器优化链路）
- 日期：2026-09-14
- 变更范围：
  - `Optimizer::PushDownAggregates`（单表裸 COUNT/SUM 聚合改写 AggregateNode → PreAggScanNode）
  - `PreAggScanNode`（`include/plan/Plan.h` / `src/plan/Plan.cpp`，继承 AggregateNode 复用下游列映射）
  - `PreAggScanExecutor`（`include/execution/PreAggScanExecutor.h` / `src/execution/PreAggScanExecutor.cpp`，扫描内累计 + 哈希分组）
  - `ExecutionEngine::BuildExecutor` 增 PRE_AGG_SCAN 分支与头文件引用
  - `tests/storage/storage_ut.cpp` 增 `TestPreAggPushDown`（52 项断言）

## 1. 构建与回归基线
- `cmake -S . -B build && cmake --build build -j 8`：0 错误 0 告警（`-Wall -Wextra`）；新 `.cpp` 由 file(GLOB) 重新 configure 拾取
- storage_ut：`checks: 292554   fails: 0   RESULT: PASS`（相对 U3-1 基线 292502 + 新增 52 项断言）
- SQL 回归：`PASSED=55 FAILED=0 ELAPSED=4.6281447s`

## 2. 单元验收 TestPreAggPushDown（storage_ut.cpp，走 Database→Planner→Optimizer→执行全链路）
| 断言 | 结果 |
|---|---|
| 合格形态改写：`SELECT COUNT(*) FROM t` EXPLAIN 含 `PreAggScan` | ✓ |
| 合格形态改写：`SELECT grp, COUNT(*), SUM(val) FROM t GROUP BY grp` EXPLAIN 含 `PreAggScan` | ✓ |
| 语义等价：`COUNT(*)`=4；`COUNT(val)`=3（NULL 不计）；`SUM(val)`=600.0 | ✓ |
| 分组输出：grp=10{2,300} / grp=20{1,SUM→NULL} / grp=30{1,300}，ORDER BY 排序正确 | ✓ |
| WHERE + GROUP BY：Filter 保留在 PreAggScan 之下，过滤后计数=2 组各 1 | ✓ |
| HAVING：Filter 在 PreAggScan 之上，`HAVING SUM(val) > 100` 过滤 20 组剩 2 组 | ✓ |
| 不合格形态零改写：AVG / COUNT(DISTINCT) / COALESCE(SUM(x),0) EXPLAIN 不含 `PreAggScan`，结果仍正确 | ✓ |
| 多表 JOIN 聚合：全 INNER 链不改写，`count(*)=2` 正确 | ✓ |
| 空表：`COUNT(*)=0`、`SUM(x)=NULL`（PreAggScan 空输入仍发射初始状态行） | ✓ |

## 3. 手动 EXPLAIN 实证（CLI 冒烟）
```
SELECT grp, COUNT(*), SUM(val) FROM t1 GROUP BY grp;
┌──────────────────────────────────────────────┐
│ Project(grp, COUNT(1), SUM(val))              │
│   PreAggScan(GROUP BY [grp], AGG [grp, COUNT(1), SUM(val)]) │
│     SeqScan(t1)                               │
└──────────────────────────────────────────────┘
结果：10|2|300、20|1|NULL、30|1|300（NULL SUM → NULL）✓
```
- `SELECT COUNT(*) FROM t1` → 4；`SELECT grp, SUM(amt) ... HAVING SUM(amt) > 0` → 10|4 ✓
- `SELECT COUNT(*) FROM t2 WHERE amt > 2.0` → 1（WHERE 先过滤后聚合）✓
- 不合格形态 EXPLAIN 保持 `Aggregate`：`AVG(amt)`、`COUNT(DISTINCT grp)`、`COALESCE(SUM(amt), 0)` 均不改写 ✓
- 空表 `SELECT COUNT(*) FROM t_empty` → 0、`SELECT SUM(x) FROM t_empty` → NULL（仍改写 PreAggScan 且语义正确）✓

## 4. 实现要点与正确性保证
- 资格判定（`IsPreAggEligible`）保守三重守底：① 子树须为单表访问路径（`SeqScan` 或 `Filter* -> SeqScan`），JOIN/Project/无 FROM 一律不改写；② 每个 aggregate_expr 须为「裸 COUNT/SUM（无 DISTINCT、参数 ≤1 且不含嵌套聚合/窗口调用）」或「不含任何聚合调用的普通表达式」；③ GROUP BY 表达式不含聚合调用（防御）。
- PreAggScanExecutor 与 AggregateExecutor 语义逐项对齐：`COUNT(*)` 计行、`COUNT(col)` 计非 NULL、`SUM(col)` 仅数值列累加且 `any_numeric` 为空时输出 NULL、空输入无 GROUP BY 仍发射初始状态行；组内样本行输出普通表达式（与 Aggregate 一致取最近一行）。
- 分组用 `unordered_map`（分组键 → 组下标）O(1) 均摊，替代 AggregateExecutor 对 groups_ 的线性扫描 + 每轮重新拼接键串的 O(组数) 比较。
- 执行引擎集成：PreAggScanNode 继承 AggregateNode，使 HAVING Filter / Project / Sort / DeriveTerminalColumns / 别名映射等既有按 AggregateNode 读取 aggregate_exprs/aliases 的下游代码透明复用；`CollectScanTableNames` 对子树递归收集单表（SEQ_SCAN/INDEX_SCAN），cmap 面向扫描列序构建。
- 与 ChooseAccessPaths 顺序：PushDownAggregates 在 ChooseAccessPaths **之前**执行（资格判定基于改写前的 Filter/SeqScan 形态），改写后子树仍可被后续索引路径改写（Filter->IndexScan），IndexScan 输出全表行故 PreAggScan 列解析不受影响。

## 5. 备选方案对比（详见《11_Phase6_U3_查询优化增强_阶段文档.md》）
| 方案 | 分组 | 额外开销 | 结论 |
|---|---|---|---|
| A：扫描内预聚合（本方案） | 哈希 O(1) | 无 | 采纳：省一次行流水、O(1) 分组 |
| B：复用 AggregateExecutor 仅改计划标记 | 线性 O(G) | 无 | 不采纳：无性能收益，无下推价值 |
| C：两阶段聚合（部分/最终） | 哈希 | 中间结果物化 + 二次扫描 | 不采纳：单线程扫描下中间态无分摊价值，复杂度高 |

## 结论
U3-2「聚合下推」实现并全量验证闭环：合格形态改写成立（EXPLAIN 断言）、语义与 AggregateExecutor 逐项等价（NULL/空表/WHERE/HAVING/ORDER BY）、不合格形态零改写零回归、UT 292554/0 fails、SQL 回归 55/55、0 告警。
