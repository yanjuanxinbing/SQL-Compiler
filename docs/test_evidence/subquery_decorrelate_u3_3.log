# U3-3 子查询去关联（相关子查询 → SEMI/ANTI JOIN + 非相关物化 + 递归 CTE 缓存失效）· 验收证据
- 项目：SQL-Compiler（编译原理 + 操作系统 + 数据库 三模块综合实训）
- 模块：编译/执行层（编译器优化链路）
- 日期：2026-09-14
- 变更范围：
  - `Optimizer::DecorrelateSubqueries` / `TryDecorrelateFilter`（`include/optimizer/Optimizer.h` / `src/optimizer/Optimizer.cpp`，Optimize 入口最先执行）
  - 辅助：`IsSubqueryDecorrelatable` / `IsSimpleSubqueryPlanShape` / `CollectInnerQualifiers` / `ExprHasNestedSubquery` / `ReferencesOuterTable` / `HasUnqualifiedColumnRef` / `CondRefsResolve` / `SplitConjuncts` / `CombineConjuncts` / `ClonePlan` / `StripCorrelatedFilters` / `CollectCorrelatedConjuncts`
  - `JoinType::SEMI/ANTI`（`include/plan/Plan.h` / `src/plan/Plan.cpp`）+ `JoinExecutor` 半/反连接执行（只输出左行）
  - `ExecutionContext::subquery_cache_` 非相关子查询物化缓存 + `SubqueryCacheStats` 观测 sink（`include/execution/Executor.h` / `src/execution/Executor.cpp` / `src/execution/ExpressionEvaluator.cpp`）
  - `CteDefineExecutor` 递归 CTE 迭代边界 `ClearSubqueryCache()`（`src/execution/CteExecutor.cpp`）
  - `Database` 级子查询缓存统计注入 + `\stats` 新增 `subquery cache` 行（`include/db/Database.h` / `src/db/Database.cpp`）
  - `tests/storage/storage_ut.cpp` 增 `TestSubqueryDecorrelation`（10 场景）+ `TestSubqueryMaterialization` + `TestRecursiveCteCacheInvalidation`
  - 新增 SQL 回归 `tests/sql/53_subquery_decorrelate.sql`（13 项用例 + `\stats` 证据）

## 1. 构建与回归基线
- `cmake -S . -B build && cmake --build build`：0 错误 0 告警（`-Wall -Wextra`，强制重编译 Optimizer/Execution/Join/CTE 相关 TU 复核）
- storage_ut：`checks: 292643   fails: 0   RESULT: PASS`（相对 U3-2 基线 292554 + 新增断言；TestOptimisticSplitConcurrency 为已知偶发 flaky，重跑通过）
- SQL 回归：`PASSED=56 FAILED=0`（新增 53_subquery_decorrelate.sql；原 55 条零回归）

## 2. 单元验收 TestSubqueryDecorrelation（Database→Planner→Optimizer→执行全链路）
| 场景 | EXPLAIN 断言 | 结果断言 |
|---|---|---|
| (1) 相关 EXISTS → SEMI | `Join(SEMI, (o.cust_id = c.id))` | 4 行（Alice..Eve，David 无订单被过滤）|
| (2) 相关 NOT EXISTS → ANTI，内层非相关子句保留 | `Join(ANTI, (o.cust_id = c.id))` + `Filter((o.amount > 100))` | 3 行（Alice/Charlie/David）|
| (3) IN → SEMI（未限定外列、内层单表）| `Join(SEMI, (id = orders.cust_id))` + `Filter((amount >= 100))` | 3 行（Alice/Bob/Eve）|
| (4) ANY → SEMI | `Join(SEMI, (id > orders.cust_id))` + `Filter((amount >= 100))` | 4 行（Bob..Eve）|
| (5) 混合合取：city='BJ' AND EXISTS | 左子保留 `Filter((city = 'BJ'))` | 2 行（Alice/Charlie）= 交集 |
| (6) NOT IN 保守不改写（NULL 语义与 ANTI 不等价）| 保持 `Filter(NOT ((id IN ...)))`，无 Join(SEMI/ANTI) | 3 行（Alice/Charlie/David）|
| (7) 非相关 EXISTS → SEMI 无条件连接 | `Join(SEMI, ...)` | 5 行（右表非空全输出）|
| (8) 空表 | EXISTS 空表 / NOT EXISTS 空表 | count=0 / count=5 |
| (9) NULL 语义：外列 NULL 经 SEMI 按 UNKNOWN 过滤 | `Join(SEMI, (id = orders.cust_id))` | 仅 'A'（NULL 行过滤）|
| (10) 嵌套相关子查询：两层均被递归去关联 | 外层 `Join(SEMI, (o.cust_id = c.id))` + 内层 `Join(SEMI, (c2.id = o.cust_id))`（SEMI 计数 ≥2）| 4 行 |

## 3. 单元验收 TestSubqueryMaterialization（非相关物化，白盒计数）
| 步骤 | SQL | 计数断言 |
|---|---|---|
| 0 | CREATE/INSERT（无子查询）| materialize=0, hits=0 |
| 1 | `SELECT name, (SELECT count(*) FROM orders) AS cnt FROM customers`（5 外层行）| materialize=1, hits=4；结果恒 6 ✓ |
| 2 | 相关标量 `(SELECT max(amount) FROM orders o WHERE o.cust_id = c.id)` | 计数不变（1/4）：相关逐行求值、不入缓存；结果逐行正确（100/200/80/NULL/300）|
| 3 | 第二条非相关 `(SELECT count(*) FROM orders o WHERE o.amount > 100)` | materialize=2, hits=8；结果 2 ✓ |
| 4 | 未限定列 `(SELECT count(*) FROM orders WHERE amount > 100)` | 计数不变：`WalkExprForOuterRefs` 对未限定列保守视为外层引用（不做 schema 解析排歧义）→ 逐行重跑不入缓存（设计取舍：宁可多跑不缓存错）|

## 4. 单元验收 TestRecursiveCteCacheInvalidation（迭代边界清缓存）
- `WITH RECURSIVE acc AS (SELECT 1 AS n UNION ALL SELECT n + (SELECT max(acc.n) FROM acc) FROM acc WHERE n < 5) SELECT count(*), max(n) FROM acc`
- 结果：`cnt=4, mx=8`（序列 1,2,4,8）；若不清缓存第 2 轮复用第 1 轮物化的 max=1 → 序列 1,2,3,4,5（cnt=5, mx=5）。
- 白盒计数：materialize=3, hits=0（4 轮迭代中第 4 轮 WHERE n<5 先过滤空输入未求值子查询；3 次真实物化证明每轮都在重新物化，而非 1 次物化 3 次命中）。

## 5. 手动 EXPLAIN 实证（CLI 冒烟）
```
EXISTS:            Project(name)  Join(SEMI, (o.cust_id = c.id))
NOT EXISTS:        Project(name)  Join(ANTI, (o.cust_id = c.id))  Filter((o.amount > 100))
IN:                Project(name)  Join(SEMI, (id = orders.cust_id))  Filter((amount >= 100))
ANY:               Project(name)  Join(SEMI, (id > orders.cust_id))  Filter((amount >= 100))
混合合取:           Project(name)  Join(SEMI, (o.cust_id = customers.id))  Filter((city = 'BJ'))
NOT IN（守底）:     Project(name)  Filter(NOT ((id IN (SELECT cust_id FROM orders WHERE (amount >= 200)))))
嵌套两层:           Project(name)  Join(SEMI, (o.cust_id = c.id))  Join(SEMI, (c2.id = o.cust_id))
```
- 结果一致性：EXISTS 4 行 / NOT EXISTS 3 行 / IN 3 行 / ANY 4 行 / 混合 2 行 / NOT IN 3 行；空表 EXISTS=0、NOT EXISTS=5；NULL 外列 IN 过滤后仅 'A' ✓

## 6. \stats 观测（53_subquery_decorrelate.sql 末尾）
```
subquery cache       : materialize=4  hits=4
```
- 组成：非相关 cnt 子查询（1 物化 + 4 命中）+ 递归 CTE max(acc.n)（3 次迭代重物化）——物化/命中与单元白盒断言同源。

## 7. 实现要点与正确性保证
- **改写形态守卫（`IsSubqueryDecorrelatable`）**：内层单表、无 GROUP BY/HAVING/ORDER BY/DISTINCT/LIMIT/派生表/JOIN；内层计划形状仅 `Project(顶层可选) → Filter* → SeqScan`；IN/ANY 另要求 select_list 为单裸列。任何疑点整项保持原 Filter 逐行求值（保守守底零回归）。
- **相关子句迁移**：`CollectCorrelatedConjuncts` 抽出 WHERE 中引用外层表的合取项 → 移入连接条件；内层计划经 `ClonePlan` 深克隆（PROJECT/FILTER/SEQ_SCAN）+ `StripCorrelatedFilters` 剥除相关子句，不污染原 subquery_plan。IN/ANY 的连接条件由「外列 op 内列」构造（未限定内列补内层表名/别名）。
- **保守守底四道闸**：NOT IN / NOT ANY（NULL 语义与 ANTI 不等价）；相关合取项含嵌套子查询（连接上下文无法提供外层绑定）；相关合取项含未限定列（首表优先解析歧义）；`CondRefsResolve` 无法把条件列解析到左/右子树扫描表（catalog 可用时校验列名存在）。任一闸不过 → 保持原求值路径。
- **非相关物化**：键 = `SubqueryExprNode.subquery_plan` 指针（同语句唯一）；首次求值执行一次并缓存，后续外层行 O(1) 命中；相关子查询（`IsSubqueryCorrelated`）逐行求值。未限定列保守视为外层引用 → 不入缓存（防 schema 歧义）。
- **递归 CTE 失效**：每轮迭代边界 `ClearSubqueryCache()`，递归项内引用 CTE 工作集的非相关子查询随 delta 变化重物化，杜绝陈旧结果。
- **观测**：`SubqueryCacheStats` sink（原子）注入 ExecutionEngine 默认 ctx 与会话路径 ctx，跨语句累计；`\stats` 新增 `subquery cache : materialize=N hits=M` 行。

## 8. 备选方案对比（详见《11_Phase6_U3_查询优化增强_阶段文档.md》）
| 方案 | 相关子查询 | 非相关子查询 | 结论 |
|---|---|---|---|
| A：仅物化非相关、不去关联 | 逐行重跑（N×M 代价）| 缓存 | 不采纳：相关 EXISTS/IN 是主要成本 |
| B：全量去关联（含 NOT IN、复杂内层）| 全改写 | 缓存 | 不采纳：NOT IN 的 NULL 语义与 ANTI 不等价、复杂形态判断易错，回归风险高 |
| C：本方案（安全形态改写 + 保守守底 + 物化 + CTE 失效）| 安全形态 → SEMI/ANTI；疑点保原 | 缓存 | 采纳：收益面（EXISTS/IN/ANY 主体）+ 零回归 |

## 结论
U3-3「子查询去关联」实现并全量验证闭环：相关 EXISTS/NOT EXISTS/IN/ANY 改写为 SEMI/ANTI JOIN（EXPLAIN 断言成立）、非相关子查询物化缓存（白盒计数 1 物化 4 命中、\stats 同源）、递归 CTE 迭代边界失效缓存（cnt=4/mx=8 语义正确 + materialize=3 白盒印证）、NOT IN/未限定列/嵌套疑点保守守底零回归；storage_ut 292643/0 fails、SQL 回归 56/56、0 告警。U3 全项闭环。
