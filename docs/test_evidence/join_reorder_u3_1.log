# U3-1 JOIN 顺序启发式 · 验收证据
- 项目：SQL-Compiler（编译原理 + 操作系统 + 数据库 三模块综合实训）
- 模块：编译/执行层（编译器优化链路）
- 日期：2026-09-14
- 变更范围：`Optimizer::ReorderJoins`（全 INNER 左深链小表驱动重排，3 表以上）+ `TableHeap::GetApproxRowCount`（廉价行数探针，供基数估计）

## 1. 构建与回归基线
- `cmake --build build`：0 告警（`-Wall -Wextra`）
- storage_ut：`checks: 292502   fails: 0   RESULT: PASS`（相对基线 292486 + 新增 TestJoinReorder 16 项断言）
- SQL 回归：`PASSED=55 FAILED=0`
- 注：运行中出现一次 `TestOptimisticSplitConcurrency`（并发 delete+scan）偶发失败（storage_ut.cpp:1907-2015 区间），重跑即过——为既有 documented 偶发测试，与本次 LockManager/LogManager/Optimizer 改动无关。

## 2. 单元验收 TestJoinReorder（storage_ut.cpp，走 Database→Planner→Optimizer→执行全链路）
| 断言 | 结果 |
|---|---|
| 3 表全 INNER 链小表驱动（EXPLAIN 文本 `SeqScan(t_small)` 先于 `SeqScan(t_big)`） | ✓ |
| 3 表 join 结果集与未重排语义一致（t_big:1..30 ⋈ t_med:1..10 ⋈ t_small:1..3 → count=3） | ✓ |
| 含 OUTER 链不被误重排（all-INNER 守卫）且结果正确（LEFT 补空 count=3） | ✓ |

## 3. 手动 EXPLAIN 实证（3 表与 4 表）
规模 t_big=30 / t_med=10 / t_small=3，重排后：
```
Project(t_big.a, t_med.b, t_small.c)
  Join(INNER, (t_big.a = t_med.b))
    Join(INNER, (t_med.b = t_small.c))
      SeqScan(t_small)   <- 最小驱动在前
      SeqScan(t_med)
    SeqScan(t_big)       <- 最大最后接入
```
4 表 t1=20/t2=10/t3=5/t4=3 → `SeqScan(t4)/SeqScan(t3)/SeqScan(t2)/SeqScan(t1)` 由小到大，count=3 正确。

## 4. 实现要点与正确性保证
- 仅对「全 INNER + 左深 + 右子恒为 SeqScan + 条件全限定列」的链重排；含 OUTER/非限定列/无法归属/探针失败一律保持原顺序（零回归守底）。
- 就近贪心：以最小基数表为种子，逐次接入「与已放置集合有 join 边」的最小基数表，避免中间笛卡尔积。
- 条件重挂：每个条件挂在「其引用表里新位置最大」的 spine 级（该级引入该表）；全 INNER 下左右次序不影响结果集，只影响中间集大小，故挂法与顺序解耦。
- 列定位一致性：执行引擎在翻译阶段用「重排后」同一棵计划经 `CollectScanTableNames`（左深前序）构建 column_index_map，与 JoinExecutor 的 left||right 拼接列序天然一致，重排不破坏上层 Filter/Project 列解析。
- 估计基数 = 每表页链 slot_count 之和（含墓碑/MVCC 版本槽的粗估，仅用于相对排序）；当前无表内 Filter 下推，选择性取 1。

## 结论
U3-1「JOIN 顺序启发式」实现并全量验证闭环：EXPLAIN 断言成立、多表（3/4 表）重排生效、结果一致、OUTER 守卫不误伤、质量基线不回归。