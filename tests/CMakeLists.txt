# 测试目录占位配置
#
# TODO: 后续可在此接入测试框架（如 GoogleTest / Catch2），例如：
#
#   find_package(GTest REQUIRED)
#   add_executable(lexer_test lexer_test.cpp)
#   target_link_libraries(lexer_test PRIVATE sqlcompiler_lib GTest::gtest_main)
#   add_test(NAME LexerTest COMMAND lexer_test)
#
# 目前暂不包含任何测试目标，避免在功能未实现时构建失败。
